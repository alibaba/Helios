[project]
name = "security-mcp"
version = "0.1.0"
description = "MCP server for code security analysis"
authors = [
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp",
    "openai",
]
